{
  "name": "p03_gbl",
  "version": "1.0.0",
  "description": "**Description**",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "test": "jest --config test/config/jest.config.js",
    "test-detailed": "jest --config test/config/jest.config.js --watchAll --verbose --coverage",
    "e2e-open": "cypress open --project test --config-file config/cypress-detailed.config.js",
    "e2e": "cypress run --project test --config-file config/cypress.config.js --headless"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/cypress": "^1.1.3",
    "@types/jest": "^29.5.4",
    "cypress": "^12.17.1",
    "jest": "^29.6.3",
    "ts-jest": "^29.1.1",
    "vite": "^2.3.5"
  }
}
